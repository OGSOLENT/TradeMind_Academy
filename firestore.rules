rules_version = '2';

// Deny-by-default (guardrail §7.5). Real rules + tests land in Phase 2.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
