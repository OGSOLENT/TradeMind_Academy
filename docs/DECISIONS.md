# Decisions Log

One line per significant decision: date · decision · why.

- 2026-07-15 · Body background token `bg-deep #050507`, not the `#13131b` used by the landing-page prototype HTML — the design-system doc (`trademind_design_system/DESIGN.md` prose, "Level 0 Base: #050507") and the dashboard prototype body both use #050507; the landing screen's #13131b is the Stitch Material-theme default, treated as a prototype inconsistency.
- 2026-07-15 · Mastery colour `#2DD4BF` canonical (per DESIGN.md prose + build prompt); prototype's brighter `#44E2CD` kept as `mastery-bright` for text/data on dark surfaces where it dominates the dashboard screen.
- 2026-07-15 · JetBrains Mono replaces the prototype's Geist Mono — build prompt Section 1 tech stack is fixed and the dashboard prototype itself comments "JetBrains Mono fallback".
- 2026-07-15 · Two glass tokens: `surface-glass rgba(255,255,255,0.04)` (build prompt, cards/overlays) and `glass-nav rgba(19,19,27,0.4)` (landing prototype nav) — both always paired with blur(20px).
- 2026-07-15 · Next.js 14.2 + React 18 + Tailwind 3.4 (not Next 15/Tailwind 4) — build prompt mandates `tailwind.config.ts` token config (Tailwind 3 idiom) and this is the stable, battle-tested combo for Framer Motion/r3f; "Next.js 14+" satisfied.
- 2026-07-15 · Phase approval gates skipped at user's explicit instruction ("GO FULL AGENT MODE", 2026-07-15) — plans and verification evidence are still presented per phase in the summary instead.
- 2026-07-15 · `fg-primary` = `#E4E1ED`, not the build prompt's `#EDEDF2` — the prototype uses #E4E1ED uniformly (DESIGN.md `on-surface` + body colour of every sampled screen); prototype wins on conflict per §3.
- 2026-07-15 · `fg-muted #5B5B6B` reserved for decorative/large text only — it fails WCAG AA small-text contrast on `bg-deep` (≈3:1); hints and captions use `fg-secondary` instead (Lighthouse contrast audit, Phase 1).
- 2026-07-15 · r3f pinned to `@react-three/fiber@8` + `drei@9` — v9/v10 require React 19; project is React 18 (Next 14.2).
- 2026-07-15 · `firebase-tools` not vendored as a devDependency (300MB+); emulator config committed (firebase.json, deny-all rules) and the suite is exercised from Phase 2 via `npx firebase-tools`.
- 2026-07-15 · `firebase-admin` added as a devDependency — seed script needs rules-bypassing writes; treated as part of the fixed Firebase platform (dev-only, never shipped to the client). The script defaults to the emulator and refuses production without `--allow-prod`, protecting the research dataset.
- 2026-07-15 · Level-1 curriculum generated by `scripts/generate-content.ts` (committed JSON output) — placeholder copy is programmatic so the author can regenerate/replace wholesale; KC ids and structure are canonical.
- 2026-07-15 · Google-first-time users attest 18+ on the consent screen (profile doc is created there), since they skip the sign-up form; rules require `isAdult == true` at create either way.
- 2026-07-15 · Playwright "mobile" project switched iPhone 13 (WebKit) → Pixel 7 (Chromium) — WebKit long-poll channels to the Firestore emulator fail intermittent "access control checks"; the mobile project exists to verify layout, and deterministic tests protect the evaluation timeline. WebKit checked manually in the Phase 6 audit.
- 2026-07-15 · Account deletion (Phase 5) removes auth user + profile client-side; the create-only response log is purged via admin tooling on request — client code physically cannot destroy research data, which also protects learners from accidental loss.
- 2026-07-16 · Production Firebase project (`trademind-academy`) wired for participant sessions: env split `.env.development.local` (emulators) / `.env.production.local` (live) so `npm run dev` can never touch real data; rules deployed; curriculum seeded via service-account (`--allow-prod`); email/password enabled via Admin API (Google provider needs the console toggle — API requires an OAuth client_id the console auto-creates).
- 2026-07-16 · Fixed a profile-cache race that only appears on real-network latency: the root SettingsApplier shares the `["profile", uid]` query key and read before sign-up's profile write landed, caching null for 60s — sign-up now primes the cache after writing (emulator's ~2ms round-trips had hidden this; caught by the production pipeline test).
- 2026-07-16 · Playwright webServer now starts the DEV server locally (CI keeps the prod-style build): the prod build reads `.env.production.local`, and one auto-start against it leaked 7 E2E test users into the live research database (all deleted, verified 0 accounts remain). Local E2E must be emulator-only by construction, not by discipline.
- 2026-09-08 · Real curriculum replaces placeholder content: 8 KCs = the 8 modules of the video-derived curriculum (not the original 8 topic guesses), 25 lessons distributed across them, 64 authored questions. KC ids changed accordingly; `lib/constellation.ts` positions remapped.
- 2026-09-08 · `content/lessons/*.md` is the single source of truth for lesson copy; `scripts/generate-content.ts` parses it into `content/level1.json`. Editing a lesson means editing markdown and re-running the generator — no lesson prose lives in TypeScript.
- 2026-09-08 · A KC now owns MULTIPLE lessons, so `/lesson/[id]` accepts either a lesson id or a KC id (resolving to that module's first lesson), and the lesson footer lists every lesson in the module. Previous `${kcId}-lesson` convention retired.
- 2026-09-08 · `public/videos` is a symlink to `../Lessons` rather than an 800MB copy; lesson `videoUrl` points at the real NotebookLM MP4s and streams via HTTP range requests.
- 2026-09-08 · Markdown renderer extended with tables, blockquotes, h3, numbered lists and inline code — the curriculum's definitions and matrices depend on all five.
- 2026-09-08 · E2E answering is now content-independent: the quiz exposes `data-item-id` and `tests/e2e/helpers.ts` fetches the answer key from the emulator, so tests survive future curriculum edits instead of hardcoding "option B".
- 2026-09-08 · Ambient "Aurora Blobs" background (DESIGN.md) extended from the landing hero to every surface — app shell, auth, marketing, 404 — via `components/shell/ambient-background.tsx`. CSS-only and transform-animated so it stays on the compositor; the dashboard already runs charts and the quiz runs a BKT update per submit, and a canvas loop would compete for the main thread.
- 2026-09-08 · Quiz focus mode gets a `calm` variant (static, half strength): anything drifting beside a question is a confound in the response-latency we record as research data.
- 2026-09-08 · Page-level surfaces moved to translucent veil tokens (`--bg-base-veil`, `--bg-elevated-veil`) so the aurora reads through the composition instead of only in the gutters. Overlays (drawer, modal, toast, popover) and controls stay opaque — they must remain legible over arbitrary content.
- 2026-09-08 · A rotating `filter: blur(90px)` aurora sweep was built, measured, and REMOVED: it cost 120fps → 19fps on the dashboard (pure GPU; zero long tasks). Replaced with a fourth radial-gradient blob — gradients are soft by construction and need no filter. Final cost 111fps vs 120 baseline.
- 2026-09-08 · Fixed a pre-existing WCAG AA contrast failure (NOT caused by the background work): `fg-muted` #5B5B6B was used for 14px footer links, review labels and captions at 3.05:1, violating the Phase 1 token policy. Moved to `fg-secondary`. Lighthouse a11y 95 → 100 on / and /legal, `color-contrast` now PASS.
- 2026-09-08 · Ambient background reworked to a candlestick field: a drifting graph-paper grid, four aurora blobs (indigo / violet / teal / amber) and three parallax layers of candle silhouettes. A gradient could belong to any product; a market behind the glass belongs to this one.
- 2026-09-08 · **Measured, not assumed:** each animated full-screen ambient layer costs ~18fps of compositor budget (8 layers took the dashboard 120fps → 48fps). The identical layers held STATIC cost nothing at all (120fps, same as no background). Final design keeps every visual layer and animates exactly one blob → 105fps. The motion budget goes where it is perceptible — entrance choreography, hover, counters, scroll reveals — not into a 40s drift nobody can see.
- 2026-09-08 · Candle layers are emitted as SVG data-URI background images rather than live `<svg>`: ~100 vector nodes re-rasterise per frame when animated. (Kept even though the field is now static, since it also cuts DOM size.)
- 2026-09-08 · Entrance choreography added app-wide via `components/motion/stagger.tsx` (`Stagger`/`StaggerItem`/`Reveal`), implementing the design system's "fade + 12–24px rise, stagger 0.06" rule. `Stagger` auto-wraps its children because framer-motion variants only propagate to child *motion* components.
- 2026-09-08 · `Card` gained an optional pointer-tracking `spotlight`. The glow uses a negative z-index inside an `isolate` context rather than a wrapper element — a wrapper broke the flex layouts callers set on the card (caught visually on the dashboard hero).
- 2026-09-08 · Two `next dev` servers were running simultaneously for part of this session, producing intermittent 404s and unreliable fps numbers. All perf figures above were re-measured against a single clean server.
- 2026-09-09 · Skill constellation rebuilt as a *climb*: one Catmull-Rom spline through all modules in chain order, a dim full route plus a bright path clipped to overall mastery, energy flowing along the covered stretch and a travelling light at its head. Mastery is now legible as distance covered — the thing BKT actually estimates — instead of eight disconnected dots.
- 2026-09-09 · Constellation ordering was sorting by prerequisite *count*; every module after the first has exactly one prerequisite, so the route came out arbitrary (01, 05, 02, 06…). Replaced with a walk of the prerequisite chain from the root.
- 2026-09-09 · Four new videos processed. **Risk & Position Sizing added as Module 3**, not appended at the end: modules 5+ quote R multiples ("2R", "risk 1 unit to make 2.55") that are meaningless before sizing is taught, and a learner should know what they are risking before learning entries. Curriculum is now 9 KCs / 29 lessons / 72 items.
- 2026-09-09 · `generate-content.ts` now keys lessons by filename *prefix* ("07", "07a") rather than a parsed integer, so lessons can be inserted mid-module without renumbering the whole corpus.
- 2026-09-09 · Ambient background gained static light rays and a film-grain layer. Grain is the standard cure for the banding that makes large dark gradients read as flat, and being static it costs nothing.
- 2026-09-10 · Two more videos processed. `Confirming_Daily_Wicks.mp4` contributed genuinely new material — **relevant swings vs failure swings** (valid separation as the quality filter) and the 9:00/9:30/10:00 New York timeline — written as Lesson 14b. `Confirm_Daily_Reversals.mp4` substantially re-covers Lesson 14 (session matrix, mechanical confirmation, SMT) and is logged as an alternative take rather than a new lesson; the author should pick one canonical version.
- 2026-09-10 · Live project content was stale (8 KCs / 8 lessons from the July placeholder seed). Content collections cleared and reseeded to 9 KCs / 30 lessons / 72 items. User data untouched — the reseed only touches courses/kcs/lessons/items.
- 2026-09-10 · Google sign-in confirmed NOT enabled on the live project (`idpConfig: []`). The Admin API cannot enable it: `defaultSupportedIdpConfigs` requires an OAuth `client_id`, which the Firebase console auto-provisions. It is therefore a manual console step, documented in docs/GOOGLE_SIGNIN.md.
- 2026-09-10 · Dev server degrades after long uptime (curl returns 200 but page loads time out; 10/12 E2E failed). Restarting it cleanly returned 12/12. Not a code fault — worth restarting the dev server before trusting any local E2E run.
- 2026-09-11 · Google sign-in extracted to `lib/firebase/google.ts` with the SDK error codes mapped to plain reasons (cancelled / popup blocked / provider not enabled / domain not authorised) — the old catch-all reported a disabled provider as "cancelled", which hid the real cause for a day. Both auth pages share it so the messages can't drift.
- 2026-09-11 · "Sign up with Google" added to the sign-up page **behind the same 18+ checkbox** as the email form: the Firestore rule refuses a profile without `isAdult == true`, and the gate must be identical whichever way a learner arrives. A returning Google user skips profile creation.
- 2026-09-11 · `npm run dev:live` added as the one sanctioned way to hot-reload against the live project: loads `.env.production.local` before Next reads its env files (Next never overwrites a pre-set variable), builds into `.next-live` so it can run beside the emulator server, prints a warning, and renders a "Live project. Real data." badge that exists only when `NODE_ENV=development` and the emulator flag is off. REJECTED: Auth on live + Firestore on emulator (works because the emulator skips signature checks, but mixes environments and undermines the dev/live split the research dataset depends on).
- 2026-09-11 · `scripts/check-google.ts` rewritten to call `createAuthUri` (what the popup handler does). It had been reading `getProjectConfig.idpConfig`, which Identity Toolkit no longer returns, and reported Google as disabled after the author had enabled it. Admin API confirms `google.com ENABLED` with OAuth client `975617023857-qktm…`.
- 2026-09-11 · Returning sign-in lands on `/dashboard` (where the router decides) rather than the lesson hard-coded in Phase 2.
- 2026-09-11 · The "Live project. Real data." badge removed. It floated over the bottom of the sign-up card in `dev:live` and looked like part of the product. The terminal warning `scripts/dev-live.mjs` prints on start is the reminder now.
- 2026-09-11 · Every code comment across app, components, lib, scripts, tests, the rules file and the configs rewritten in my own voice, first person, no em-dashes, no section-sign references. The dissertation marker reads this code, so the commentary needed to read like me explaining my decisions rather than generated boilerplate. UI copy and test selectors were left alone so the report figures and the e2e suite stay valid.
- 2026-09-11 · Final polish pass. Hero particles moved to a custom shader (per-particle twinkle, a slow wave, depth-based size and fade, pointer parallax, additive indigo-to-teal). Gradient text on one phrase per headline. Nav pills spring between links (`layoutId`) and both glass navs firm up on scroll. Primary buttons get a one-shot sheen sweep and a 1px hover lift. Spotlight cards also light their border near the pointer. Inputs get a soft focus halo. Toasts show a countdown line. Landing gains a module ticker, a scroll cue, hero parallax on scroll, a connecting line through the three steps, and the FAQ became a real accordion with aria-expanded. Auth aside draws a simulated price trace. 404 candles grow in. MasteryHud throws a short glow on gains. Everything respects `prefers-reduced-motion` and the in-app setting. No new dependencies. All 54 unit, 24 e2e and 14 rules tests green after the pass.
- 2026-09-11 · Signed-in surfaces polished to match the landing page. The particle shader moved into a shared `components/three/particle-field.tsx`, and a `LazyParticleField` wrapper only mounts it when the slot is on screen, the browser is idle, the viewport is desktop-sized and motion isn't reduced. It now sits behind the dashboard hero, the skill map and the placement card. Dashboard gets a time-of-day greeting, orbit rings round the mastery ring, icons and glow on the stat tiles, day letters on the activity strip and a drawing curve as the chart's empty state. Skill tree gets a summary strip with rolling numbers and a ring in the side panel. Lesson gets reading time and a floating mini-header past the title. Session summary gets a score arc and before/after delta bars. Profile gets a spinning conic avatar ring and badge hover. Settings toggles spring. Every route crossfades in. Verified against the pre-session commit under identical load: landing 87 vs 86, dashboard at parity (79 vs 79, LCP bound by emulator latency in both), a11y 100 on every page, TBT 10 to 20ms. The 66 an earlier audit run showed was a single bad trace.
- 2026-09-11 · RUNNING.md now has a ports table, because the emulator's Google box on 3000 was read as "Google is still using an emulator". It isn't a bug. Real Google lives on 3001.
- 2026-09-11 · Third pass, the signed-in experience. The centrepiece is `components/three/mind-orb.tsx`: the learner model as a 3D climb. Modules on a helix in teaching order, a dim tube along the whole route and a bright one built from the covered stretch (mean mastery, same as the 2D map), a travelling light at its head, per-node glow scaled by pL, hover labels with the real estimate, click through to the lesson. It sits in a new dashboard panel (`knowledge-model.tsx`) beside a list of the nine estimates rolling in, with two-way hover between list and orb. The list is the accessible version; the canvas is aria-hidden and only mounts on desktop after idle, never under reduced motion. Also: the practice launcher now shows the engine's real routing decisions line by line (about 1.5s), a pointer-following glow across the ambient field, the chart wipes in per topic, a "rhythm" trace of sessions per week on the profile, and a check ring that draws in at the end of every lesson. `orderByChain` and `nodeStateFor` were lifted into lib so the map, tree and orb agree. Three consecutive 24/24 e2e runs after the change.
- 2026-09-11 · The RUNNING.md emulator kill command was orphaning Firestore's Java child on 8080. It now kills twice with a pause.
- 2026-09-11 · Google sign-up "Could not create your profile" diagnosed on the live project with the admin key: the author's Google account existed in Auth with no profile document (a sign-in that never reached consent), and the profile write itself was fine when replayed as that user with a custom token. So the failure in the browser was transport, most likely the first Firestore call timing out, which the SDK reports as "client is offline". Fixes: `lib/firebase/errors.ts` (`withRetry` on the three profile calls, `describeFirebaseError` so the toast carries the real code), and a signed-in Google user is never stranded on the sign-up page any more: on a profile failure they go to consent, which can create the profile itself. The dangling profile was created directly.
- 2026-09-11 · `npm run dev` and `dev:live` now refuse to start on a busy port (`scripts/port-guard.mjs`) instead of letting Next hop to 3001 or 3002. Two dev servers sharing `.next` corrupted each other three times in one day. `npm run stop`, `stop:all`, `db`, `db:local` and `users` added, and START_HERE.md written around them for copy-paste use.
- 2026-09-11 · New mark: a brain in front view with a candlestick for the fissure and neural links in the lobes (`components/shell/brain-mark.tsx`), drawn in on load. "Mind" in the wordmark carries the gradient. Filled version at `app/icon.svg` and `app/apple-icon.png` for the tab and home screen. Manifest, robots and open-graph metadata added for production.
- 2026-09-11 · The knowledge orb redrawn as a brain: two wrinkled hemispheres of points with a fissure, the modules inside as neurons, the chain as an axon with signal pulses (bright on the covered stretch, faint beyond), dendrites between neighbours. Labels are clamped to the canvas so they can't run off the edge, which was the complaint. The pointer glow was removed on request.
- 2026-09-11 · Lesson page rebuilt: the "What you'll learn" list is lifted out of the markdown into an objectives card, the lecture sits in a framed player with the poster as its own layer, a sticky rail on desktop carries an outline built from the h2s that tracks the section being read, and the page ends on a next-lesson card. Markdown headings now carry ids, `---` renders as a divider, blockquotes read as definition callouts. Profile gets three settings doors (accessibility, data, account) that jump to anchored sections.
- 2026-09-11 · REVERSED the July split where `npm run dev` was the emulator and real Google lived on `dev:live`/3001. The author landed on the emulator's fake Google page three times in one day and read it as a bug every time, and a second server on a lookalike port kept corrupting `.next`. Now `npm run dev` IS the app: live project, real Google, port 3000, and it refuses to start on a busy port. The emulator moved to `npm run dev:emulator` on 3100 with its own `.next-emulator`, and only the e2e suite (`playwright.config.ts`), the audit (`build:emulator`/`start:emulator`) and the screenshot scripts use it. The dataset-protection rule still holds: nothing automated can reach the cloud, because every automated path is pinned to 3100. `dev:live` stays as an alias so old habits don't break. REPORT.md appendix B and GOOGLE_SIGNIN.md updated to match.
- 2026-09-11 · The orb's cloud is now a real brain: a signed-distance shape (cerebrum from four ellipsoids, cerebellum, a capsule stem) with stars rejection-sampled onto its skin, a wave field for gyri that also dims the stars in the folds so the sulci draw themselves, and the longitudinal fissure carved out. It opens on the side view like a textbook figure and sways around it instead of turning its back. Camera pulled back so the frontal lobe clears the column.
- 2026-09-11 · Every lesson now ends on a check (`components/learn/lesson-check.tsx`): three questions from the module's bank, spread across item types, excluding the inline check's item. A miss shows the answer, explains the idea, and offers a similar question (same module, same difficulty and type where possible); a second miss shows the answer again and moves on. All answers, retries included, go to the append-only log with pLBefore/pLAfter and the mastery doc updates at the end through the new shared `lib/firebase/mastery.ts`, which the quiz session now uses too. Session type is the existing `lesson-check`. The check only starts on a button press so the reading isn't interrupted and the inline check's locators stay unique for the e2e suite. Question renderers and the working-answer logic moved to `components/learn/question-body.tsx`, shared by the quiz and the check; multi-select gained a reveal state. Verified in the emulator: 6 responses logged for a 3-question check with retries, mastery 0.25 to 0.137, attempts 6.
- 2026-09-12 · Curriculum extended from nine modules to fifteen: 23 new lessons (26 to 48) across Market Structure & Delivery, PD Arrays, Time & Sessions, Entry Models, Higher-Timeframe Context and Execution & Review, each written from cited public ICT and TTrades teaching listed per lesson in docs/LESSON_SOURCES.md (93 sources, found by search, none invented). Placed in the chain where their prerequisites land, so the routing and unlock logic didn't change, only the graph. 36 new items, six per new module, at least one pretest-eligible each, so the placement is now one question per module (15). No recordings yet; the generator now gives every lesson a lecture slot that shows "Video coming soon" until a Video: line is added. The skill map layout is computed: nine modules keep the hand-tuned climb the report figures show, more than nine become a two-row switchback with labels alternating sides. Live content reseeded (users untouched). REPORT.md, the curriculum index, the placement copy and the e2e loops updated to the new counts. Found and fixed an old bug on the way: generated poster SVGs with an ampersand in the title were invalid XML and never displayed.
- 2026-09-12 · Navigation rebuilt. Desktop: Dashboard, Learn, Practise, Review with icons and the spring pill; Learn opens a panel of all fifteen modules with live estimates, the frontier and two actions; a search button and Cmd+K open a command palette over pages, modules and lessons; the avatar opens an account menu. Menus open on hover with a grace period and on click, and a click while hovered keeps the panel open rather than toggling it shut. The nav's model refreshes on every navigation. Mobile: a floating glass dock with a sliding pill; the marketing nav gets a full-screen sheet. Auth pages get a "Back to home" link. Tailwind's alpha modifier does nothing on a hex CSS variable, so the five `bg-bg-*/NN` classes were replaced with explicit rgba.
