rules_version = '2';

// Deny-by-default. Lesson-video read rules land in Phase 2.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
